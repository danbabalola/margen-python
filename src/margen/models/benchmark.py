"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from margen.types import BaseModel, Nullable, OptionalNullable, UNSET, UNSET_SENTINEL
from margen.utils import validate_const
import pydantic
from pydantic import model_serializer
from pydantic.functional_validators import AfterValidator
from typing import List, Literal, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class BenchmarkTypedDict(TypedDict):
    id: str
    product: str
    r"""faces | swaps | puppets | liveness"""
    title: str
    object: Literal["benchmark"]
    description: NotRequired[Nullable[str]]
    dimensions: NotRequired[List[str]]
    r"""The queryable dimension keys for this benchmark (details via /catalog)."""


class Benchmark(BaseModel):
    id: str

    product: str
    r"""faces | swaps | puppets | liveness"""

    title: str

    object: Annotated[
        Annotated[Literal["benchmark"], AfterValidator(validate_const("benchmark"))],
        pydantic.Field(alias="object"),
    ] = "benchmark"

    description: OptionalNullable[str] = UNSET

    dimensions: Optional[List[str]] = None
    r"""The queryable dimension keys for this benchmark (details via /catalog)."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["description", "dimensions"])
        nullable_fields = set(["description"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


try:
    Benchmark.model_rebuild()
except NameError:
    pass
